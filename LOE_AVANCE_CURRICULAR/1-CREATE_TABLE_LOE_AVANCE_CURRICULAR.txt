--LOE_AVANCE_CURRICULAR_

--USE ROLE ETL_PROD;

USE DATABASE BI_CDW_PROD;
USE SCHEMA UPN_STG_SIS_BNRODS_ODSMGR;

CREATE OR REPLACE TABLE LOE_AVANCE_CURRICULAR (
	PROGRAM VARCHAR(16),
	TERM_CODE_EFF VARCHAR(8),
	AREA VARCHAR(16),
	AREA_RULE VARCHAR(16),
	AREA_RULE_DESC VARCHAR(64),
	SEQ FLOAT,
	COD_CURSO_MALLA VARCHAR(16),
	CURSO_MALLA VARCHAR(1020),
	CREDITOS FLOAT,
	REQUISITOS VARCHAR(500),
	EQUIVALENCIAS VARCHAR(500),
	PERSON_UID FLOAT,
	ID VARCHAR(16),
	ESTUDIANTE VARCHAR(500),
	SEQUENCE_NUMBER FLOAT,
	ACTIVITY_DATE TIMESTAMP_NTZ(9),
	REQ_CREDITS_OVERALL FLOAT,
	ACT_CREDITS_OVERALL FLOAT,
	SMBAOGN_MET_IND VARCHAR(1),
	COD_CURSO_CAPP VARCHAR(16),
	CURSO_CAPP VARCHAR(1020),
	GRDE_CODE VARCHAR(8),
	CREDIT_HOURS_USED FLOAT,
	NRO_VEZ FLOAT,
	ACADEMIC_PERIOD VARCHAR(8),
	CUMPLIDO NUMBER(1,0),
	MATRICULADO NUMBER(1,0),
	HABILITADO NUMBER(1,0),
	PENDIENTE NUMBER(1,0),
	COMENTARIO VARCHAR(64)
);